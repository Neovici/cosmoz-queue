---
'@neovici/cosmoz-queue': patch
---

Use Untitled UI icons (`list`, `columns-02`, `square`) for the list/split/queue view-mode tabs, and expose `--cz-queue-tab-padding` so hosts can tighten the tab row.

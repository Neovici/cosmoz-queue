import {
	columns02Icon,
	listIcon,
	squareIcon,
} from '@neovici/cosmoz-icons/untitled';

const size = { width: '18', height: '18' };

export const list = listIcon(size);
export const split = columns02Icon(size);
export const queue = squareIcon(size);

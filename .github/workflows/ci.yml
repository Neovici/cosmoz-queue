name: CI
on:
  pull_request:
    branches:
      - master
      - main
  push:
    branches:
      - beta
      - master
      - main
jobs:
  build:
    uses: Neovici/cfg/.github/workflows/forge.yml@master
    secrets: inherit
